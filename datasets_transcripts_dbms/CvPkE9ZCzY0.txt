so what is SQL SQL is a structure query language this the query language is used to store the databases using relational database the relational database it is called as SQL what is ddl dmldt can I explain in detail ddl is data definition language in this data is defined by a particular commands and the commands we use in this are create alter and drop and now coming to DML DML is a data manipulation language in this we manipulate the data by using three commands insert delete and update and coming to DCL DCL is a data control language in this we in this we uh we have two commands and we those commands are Grant and revoked