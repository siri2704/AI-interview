let's talk about how to choose a database in a system design interview these databases are split into SQL and no SQL types and they each have their own strengths and weaknesses SQL databases are table based while no SQL are document key value graph or wide column stores SQL databases are vertically scalable while no SQL databases are horizontally scalable SQL databases are relational databases which means that they allow easy querying on relationships between data among multiple tables table relationships are really important for effectively organizing and structuring a lot of