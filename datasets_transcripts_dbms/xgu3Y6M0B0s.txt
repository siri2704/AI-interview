a database management system otherwise known as a database engine this is the software that database professionals use to create databases in all the objects within think of a database management system as a container for databases and there are lots of dbms's out there some popular ones here are MySQL postgress SQL and Microsoft SQL Server a dbms is a container for databases and a database is a container for data as well as the objects used to support and manage that data one common thing all dbms's share SQL I've been saying it the cool way SQL but it stands for structured query language it's the language of love for database Management Systems it's how we talk to databases and everybody uses SQL either directly or indirectly becoming a database professional is all about learning the structured query language which is actually a pretty easy language to get the hangup is all these database Management Systems Support the SQL standard