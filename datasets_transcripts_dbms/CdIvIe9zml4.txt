dbms versus rdbms so dbms is a system software which is meant to create update retrieve data in a database or from a database so now rdbms is the same but in this case you are allowed to create and manage complete database itself now talking about data we have to store it somewhere so in dbms the data is stored in files but in rd payments the data is stored in a tabular format for easy retrieval now looking at that we also have to fetch the data right in dbms you will have to fetch it directly from the files which is much slower than switching the data from rdbms in terms it's a tabular format and that will be much faster to retrieve data from it now looking at the examples of dbms so xml windows registry and also file system is basically an example now talking about relational database systems so in this case mysql oracle database there is sql server and there are so many relational databases in place so for more such comparisons follow skill