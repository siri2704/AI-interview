what is transaction in SQL databases let's have a look transaction can be defined as a SQL statement or a group of SQL statements that can be bundled logically as a single unit of work because it is a single unit of work which means if any SQL statement in a transaction fails then the entire transaction fails but when should we use a SQL transaction if you want to perform an operation against the SQL database that includes multiple actions and it's given that the operation should either be performed completely or should not be carried out at all you can Implement that operation as a SQL transaction but why should we use a SQL transaction in first place because they have four properties that help in maintaining consistent data deal with failures and more these four properties are famously known as asset properties of transactions but that's for the next video if you found the video to be informative please like this video subscribe to my YouTube channel and follow me on LinkedIn for more such fun