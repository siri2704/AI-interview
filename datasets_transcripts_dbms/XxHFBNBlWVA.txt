database is not same as dbms the problem is when you talk about the language like SQL so you say using SQL we interact with the database Now using SQL we interact with the dbms which is your rdbms so DB is basically your database is only a place where you store data the collection of data but when you want to do some operations when you want to delete data when you want to fetch data when you want to do any kind of operation as a programmer or as a person you interact with the dbms and dbms interact with the database and the language which you use with to interact with your dbms is SQL