what are the differences between SQL database and nosql database SQL stands for a standard query language SQL is a relational database management system in which data is stored in tables with rows and columns but no SQL is a non-relational database in which data is restored in some collections of Json like documents as key values SQL has more data types than no Square SQL is optimized for complex joins and transactions and no SQL is optimized for a scalability and high performance when to use SQL when we need a structured data with relationships complex queries transactions and acid when to use nosql when we need unstructured or semi-structured data flexible schema easier scalability high performance distributed environments