a database model defines the logical design structure of a database and defines how data will be stored assessed and updated in a database management system database defense model then ndt relationship model then a relational model hierarchical model network model entity relationship model relational model this database model organize data into a tree-like structure with a single root to then a child node will only have a single parallel this model efficiently describes many real-world relationships like index of a book recipes etc recipes this is an extension of the hierarchical model hierarchical model or extension network in this model data is organized more like a graph and are allowed to have more than one parameter organization foreign in this database model relationships are created by dividing object of interest into entity and its characteristics foreign [Music] relationship foreign but this model was introduced by ef coding 1970 and since then it has been the most widely used database model [Music] model