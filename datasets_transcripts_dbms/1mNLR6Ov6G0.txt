SQL versus nosql 5 things you should know about the most obvious thing is that SQL is used for relational databases and no SQL is used for non-relational databases now data in a SQL database is always structured and stored in tables in a nosql database data is unstructured and stored in Json files but relationships are still possible in a graph based nosql database we can write complex queries in SQL but it can't be done in nosql because joins don't exist now the previous Point helps me prove the fourth point that is SQL is best for transactional and oltp systems due to its querying abilities nosql can be used for low latency workloads and finally SQL has a schema bound to relationship but nosql has flexible schemas that enable faster development follow scalar for more insightful content