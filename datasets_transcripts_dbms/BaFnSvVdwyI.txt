serializability in dbms serializability is a concept that guarantees the transactions can be executed concurrently but their outcome is equivalent to the execution of transactions in a Serial manner this means we will be running non-serial transactions together without affecting one another transaction and their outcome would be the same as if we ran two serial transactions together so where is it needed this is important in multi-user systems where multiple transactions may be accessing and modifying the same data simultaneously so how is it achieved there are two approaches to achieving serializability Number One locking based methods in this we use locks to restrict concurrent access to Shared data number two optimistic concurrency control it allows concurrent access to data and only detects conflicts when transactions attempt to commit for more revision videos subscribe to curricular Pro